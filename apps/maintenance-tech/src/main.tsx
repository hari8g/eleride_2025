import React from "react";
import ReactDOM from "react-dom/client";
import { MaintTechApp } from "./ui/MaintTechApp";
import "./ui/styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MaintTechApp />
  </React.StrictMode>,
);


