import React from "react";
import ReactDOM from "react-dom/client";
import { FleetPortalApp } from "./ui/FleetPortalApp";
import "./ui/styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FleetPortalApp />
  </React.StrictMode>,
);


