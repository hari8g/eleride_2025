import React from "react";
import ReactDOM from "react-dom/client";
import { FinancingPortalApp } from "./ui/LeasingPortalApp";
import "./ui/styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <FinancingPortalApp />
  </React.StrictMode>,
);


